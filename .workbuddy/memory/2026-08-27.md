# 2026-08-27 Mini_hot 工作日志

## 上午 — 文档漂移修复
- 3 处 DSH「快捷按钮」→「better-sidebar Tab」文档漂移修复：
  - `dsh-plugin/lib/index.js`（注释 + 运行日志）
  - `dsh-plugin/dsh.plugin.json` description
  - `PLAN/plan4_DSH集成方案.md`（方案第 2 节 / 进度表 / 风险依赖 3 处）
  - 顺带 `NOTEBOOK/progress.md` 顶部加 lat.md 澄清（项目内无 lat.md 引用，属 fitness-tracker）
- 未提交（git 工作区修改态），待与主人确认提交策略。

## 下午 — 安卓壳（Capacitor 6）端到端落地
主人指示"先做安卓软件吧，DSH 我还在用" → 推进三态阶段 2，DSH/部署相关一律不动。

- 装 `@capacitor/core@6.2.1` + `@capacitor/android@6.2.1` + (dev)`@capacitor/cli@6.2.1`。
- 写 `capacitor.config.ts`（appId=com.minihot.app, appName=迷你热榜, webDir=dist, androidScheme=https）；补 `.gitignore` 忽略 android 原生构建产物。
- `npx cap add android` 生成 android/ 原生工程；`npm run build`（tsc 0 + vite 2.99s）+ `npx cap sync android`。
- **踩坑**：Gradle 分发 `services.gradle.org` 下载超时（Java 不读 Clash TUN）→ 改 `gradle-wrapper.properties` distributionUrl 到**腾讯云镜像**（国内直连 206），networkTimeout 10s→60s。
- **再踩坑**：`npx cap build android` 默认走 release 并强制要 keystore 签名 → 改用 `./gradlew assembleDebug`（49s BUILD SUCCESSFUL，无签名）。
- **端到端验证**：AVD `Medium_Phone_API_36`(API 36) 30s 开机 → adb install Success → am start 无 crash → screencap 截图确认 UI 完整渲染（标题/6 平台 Tab/微博 13+ 条数据/热值归一化/刷新主题收藏全通）。
- 产物：`D:\Mini_hot\android\app\build\outputs\apk\debug\app-debug.apk` **3.81 MB**。
- 文档更新：README 三态表 Android ✅ / plan3 阶段 2 ✅+修订历史 / progress.md 追加 2026-08-27 安卓段（含与 2026-08-26 深夜段的澄清）。

## 待办
- release 签名（keystore + signingConfigs + cap build）。
- git commit（安卓改动 + 上午文档漂移修复一并提交，等主人拍板）。
- vite.config watch.ignored 建议补 `android/` 与 `.vite/`（防 dev 期间 EBUSY）—— 本次未改，留作后续。
- 真机机型覆盖测试（仅模拟器 API 36 验证）。
